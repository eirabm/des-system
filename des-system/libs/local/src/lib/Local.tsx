import './Local.module.scss';

/* eslint-disable-next-line */
export interface LocalProps {}

export function Local(props: LocalProps) {
  return (
    <div>
      <h1>Welcome to Local!</h1>
    </div>
  );
}

export default Local;
